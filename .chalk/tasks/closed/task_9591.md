---
id: task_9591
title: CTO decision brief: 3-way design reconsideration (shim vs rootless-podman/gVisor vs single-userns-container)
type: task
status: closed
priority: 2
labels: []
blocked_by: []
parent: null
remote_task_url: null
created_at: 2026-07-18T22:45:17Z
updated_at: 2026-07-19T22:56:54Z
---

## 2026-07-19 — brief prepared for final CTO review

PR #43 ("rewrite CTO decision brief as one coherent argument") landed on main mid-session and
already did the structural cleanup (single recommendation, numbered sections, condensed
appendix). Kept that merged version as the base and layered in the two things a decision
meeting still needed:

- [x] **"Decisions requested"** block up front — 4 explicit asks: approve the §5 target
      architecture (retiring the M6-E shim direction), approve the libkrun spike as the single
      gate (Firecracker fallback), accept tier-conditional isolation depth on KVM-less hosts,
      confirm gVisor stays deferred.
- [x] **NEW §8 "Implementation scope and effort"** grounded in current code (drafted by a
      sonnet subagent from the actual sources): reused-unchanged / modified / net-new file
      lists, 5-phase plan, effort ballpark ≈ M3+M4+M7 combined, biggest uncertainty = libkrun
      as drop-in OCI runtime (the same question the spike gate answers).

Status: awaiting CTO decision (task stays in_progress until the decision lands).


## Outcome (2026-07-19) — CTO approved with edits

The CTO approved the §5 recommendation (rootless KVM micro-VM on B's rootless substrate, tiered
ladder, libkrun spike as the gate, gVisor deferred), with four binding edits:

1. §2b secret-split fix is a **merge blocker** — orchestrator ⟂ gateway uid separation holds in
   every tier from the first landed commit; never on main, even transiently, may the provider
   key sit in the untrusted-input parser.
2. **Spike-failure decision rule** — libkrun failure does NOT auto-proceed to Firecracker-direct;
   that scope change returns to the CTO with an updated estimate. Spike timeboxed to two weeks
   including the 16 KB-page arm64 box.
3. **Floor invariant gets a regression test** — CI/preflight asserts the crun tier's flag set is
   byte-for-byte today's hardened posture.
4. **No tier in the public PR footer** (attacker recon) — /healthz and operator logs only.

Plus: guest-side vsock client named as a static Go binary built in our CI, cosign-covered; and a
follow-on question on wider Go migration scope (answered in decision_aa2d under epic_6955).

Implementation plan tracked in **epic_59b1** (Milestone 8 — rootless micro-VM reviewer sandbox)
and **epic_6955** (Go adoption). The decision is recorded at the top of
docs/design/cto-decision-brief.md. M6-E shim direction retired (task_0f51 closed).
