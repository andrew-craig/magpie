# Build trees + logs are throwaway; provision.sh reproduces them.
libkrunfw/
libkrun/
crun/
*.log

# front-end experiment artifacts (throwaway)
rootfs/
magpie-krun-launch

# vsock round-trip test artifacts
host-listener.out
